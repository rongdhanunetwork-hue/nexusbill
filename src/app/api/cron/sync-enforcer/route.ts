import { NextRequest, NextResponse } from "next/server"; import { checkAndSuspendExpiredUsers, trackCustomerDataUsage } from "@/lib/sync"; export async function GET(req: NextRequest) { try { await checkAndSuspendExpiredUsers(); await trackCustomerDataUsage(); return NextResponse.json({ success: true, message: "Sync enforcer executed successfully." }); } catch (error) { return NextResponse.json({ success: false, error: String(error) }, { status: 500 }); } } export async function POST(req: NextRequest) { return GET(req); }
